spring:
  application:
    name: estante
  datasource:
    username: myuser
    password: secret
    url: jdbc:postgresql://localhost:5434/mydatabase?useSSL=false&server
    driver-class-name: org.postgresql.Driver